function pctOverThresh = computeMatrixPctOverThresh(xIn, thresh)
% pctOverThresh = computeMatrixPctOverThresh(xIn, thresh)
% -------------------------------------------------
% Blair - March 19, 2024
%
% This matrix computes the percent of (magnitude) values in a matrix that
% are over a specified threshold. 